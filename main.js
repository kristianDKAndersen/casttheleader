//import uiClicks from './src/js/uiclicks.js'; // Ensure the casing matches the file name
//uiClicks();
import sender from './src/js/sender.js'; // Ensure the casing matches the file name

document.addEventListener('DOMContentLoaded', () => {
  sender();
});
